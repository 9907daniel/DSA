def solution(s):
    answer = 0
    
    results = []
    
    for a in range(2, len(s)//2):   # 문자열 길이 (2개, 3개 ...)
        for b in range(len(s)):     # 문자열 탐색 시작 지점 (abc, bca, ...)
            tmp = ""
            lookfor = ""
            count = 0
            
            
            
            for c in range(len(s)): # 실제 문자열 확인
                if count == a:
                    if 
                    
                else:
                    lookfor += s[c]
    return answer